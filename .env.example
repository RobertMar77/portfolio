# Leave empty in Phase 1 — the frontend runs in demo mode with no backend.
# When the backend is deployed, set this to the Render URL, e.g.:
# VITE_API_URL=https://portfolio-backend.onrender.com
VITE_API_URL=
